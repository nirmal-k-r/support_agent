python-dotenv
fastapi
uvicorn[standard]
langchain
langchain-ollama
langchain-openai
# The server imports mcp.server.fastmcp.FastMCP, which is the MCP 1.x API.
# Keep the client and spawned stdio server on the same compatible major release.
mcp==1.28.1
